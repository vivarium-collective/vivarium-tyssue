# Generated by differential_adhesion.ipynb — never track or push these.
# data/ holds the spherical mesh the notebook generates on its first run;
# outputs/ holds the gifs, stills, figures, the bigraph and the CSVs.
data/
outputs/
*.log
