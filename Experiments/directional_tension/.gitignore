# Generated by directional_tension.ipynb — never track or push these.
# data/ holds the 2D hexagonal patch the notebook generates on its first run;
# outputs/ holds the gifs, stills, figures, the bigraph and the CSVs.
data/
outputs/
*.log
