# Generated by stochastic_tension.py — never track or push these.
# The input mesh (data/) is a copy of the tracked workspace/datasets/test_square.hf5,
# recreated on first run; outputs/ holds all simulation data, videos and figures.
data/
outputs/
*.log
